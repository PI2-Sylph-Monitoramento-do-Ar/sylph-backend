export type Totem = {
    _id: string,
    mac_addres: string,
    is_active: boolean,
    location: number[]
}

export type TotemDto = {
    mac_addres: string,
    is_active: boolean,
    location: number[]
}